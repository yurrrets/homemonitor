config = {
    httpOptions: {
        hostname: 'whatever.com',
        // port: 443,
        path: '/todos',
        method: 'POST'
    },
    messageAuth: {
        key: 'abcdef'
    }
}

module.exports.config = config